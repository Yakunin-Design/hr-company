// import {send_email, generate_code} from "../lib/codes";
// const code = generate_code();
// const msg = `Your code: ${code}`;
// const email = "e.yakunin04@gmail.com"
// const mail_result = send_email(email, msg);
// console.log(mail_result);

const object = {
    data1: "value1",
    data2: "value2",
    data3: "value3"
}
const filter1 = {$set: {}};

const keys = Object.keys(object)
let new_object = {};

for(let i = 0; i < keys.length; i++) {    
    //new_object.
}